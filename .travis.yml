language: python
python:
  - "2.7"
  - "3.6"
install:
    - pip install -r requirements.txt
    - pip install braintree coveralls mock mysqlclient
env:
  - DB=sqlite
  
addons:
  
script:
  - coverage run --omit=*/tests/* --source= nosetests --with-coverage
after_success: coveralls
cache: pip